interface Activity {
    fileName: string;
    htmlContent: string;
    date: Date;
}

interface MeetingNotes {
    FileName: string;
    HtmlContent: string;
    date: Date;
}

interface Officer {
    FileName: string;
    HtmlContent: string;
    Position: string;
}

interface Policy {
    FileName: string;
    HtmlContent: string;
}

interface SigInfo {
    FileName: string;
    HtmlContent: string;
    MeetingTime: Date;
}
